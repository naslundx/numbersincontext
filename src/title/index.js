import "./title.css"

const Title = () => {
    return (
        <div className="title transparentbox">
            <h1>Numbers in context</h1>
            <p>It is hard to estimate a number.</p>
            <p>This "dictionary" allows you to put it in a context.</p>
        </div>
    );
}

export default Title;